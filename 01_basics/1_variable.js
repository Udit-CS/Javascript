const accountId = 956805
let accountEmail="ujjawal@gmail.com"
var acccountPassword="1234598"
accountCity = "uttar pradesh" // this case is also happens but don't use it professonally . use var to assign the variable data.


// in case of js let is used because in let keyword no issue of scope is raised. thatswhy var is not in used now a days.

// accountId = 3456 // not allowed because it is contant.

console.log(accountId);
// if we want to print all the data then we use console.table
console.table([accountId,acccountPassword,accountEmail,accountCity]);